import React from 'react';
import { AppBar, Toolbar } from '@mui/material';

export default function Navbar() {
    return (
        <AppBar>
            <Toolbar>

            </Toolbar>
        </AppBar>
    )
}
